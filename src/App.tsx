import React, { ReactElement } from "react";
import Router from "router";
import "./App.css";

function App(): ReactElement {
  return (
    <div className="App">
      <Router />
    </div>
  );
}

export default App;
